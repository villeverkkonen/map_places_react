
const initialState = {
    markers: [
        {
            title: "testiMarker"
        }
    ]
}

const MarkerReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default MarkerReducer